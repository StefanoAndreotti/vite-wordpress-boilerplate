# Local development domain (used by Vite proxy)
LOCAL_DOMAIN=domain.com.l

# WordPress theme folder name
THEME_NAME=default-theme

# ACF PRO license key (used in auth.json, do not commit auth.json)
ACF_PRO_KEY=''

# Set to true in wp-config.php for local development, false/remove for production
# define('VITE_DEV', true);
